## Space Complexity

Space complexity measures EXTRA memory used by the algorithm.

### Important Rules
- Input array is NOT counted
- Extra arrays count as O(n)
- Recursion stack counts
- Space = maximum memory at any moment

### Examples
- int x → O(1)
- int[] arr = new int[n] → O(n)
